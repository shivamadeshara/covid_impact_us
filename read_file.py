# If you haven't installed pip --> install via Homebrew(brew install pip)
# Installation Command: run it in terminal(pip install pandas)
import pandas as pd

# Replace the given path with that of whichever file you want to read in
file = pd.read_csv("/Users/vishalbansal/Downloads/Shutdown-Reopen Dates & Parameters.csv")

# Place functions to perform on "file" here

